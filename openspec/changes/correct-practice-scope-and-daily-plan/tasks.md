- [ ] Add shared graph/list selection and a `practice-selected` handoff without
      changing existing read-only navigation semantics.
- [ ] Require non-empty scope and exactly one practice mode; remove implicit
      weak-item/full-pool pulls and mode fallback.
- [ ] Add real-goal cards and a deterministic coarse queue capped at three
      items; keep no-goal output truthful and zero-write.
- [ ] Permit Agent selection replacement only for explicit practice intent and
      preserve ordinary-conversation read-only behavior.
- [ ] Add route, domain, API, and browser tests for empty scope, replacement,
      mode filtering, no fallback, deduplication, truthful goals, and queue
      handoff.
- [ ] Run strict OpenSpec validation and the repository verification commands;
      record outputs in the Task 1 report.

