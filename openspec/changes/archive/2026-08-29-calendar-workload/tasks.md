# calendar-workload 任务

## 1. 端点

- [ ] `queries.calendar_view(pool, workspace)`：goals + 14 天直方图。
- [ ] `GET /api/w/{name}/calendar` 路由。
- [ ] 单测：分桶含方向行、目标透传、空数据。

## 2. 复习页「时间安排」区块

- [ ] 月历网格 + 目标落格（重叠堆叠、今天高亮、无截止目标一行文字）。
- [ ] 14 天柱状条 + 「偏重」标记 + 对话输入预填按钮。
- [ ] CSS（网格/柱/偏重标记，仅既有令牌）与空状态。
- [ ] Node UI 测试：落格、偏重标记、预填点击、空状态。

## 3. 验收

- [ ] 全量测试 + strict 校验。
- [ ] scratch 真实走查：2 目标 + 49 条调度下月历与曲线真实观感截图自检。
- [ ] 归档变更 + changelog。
