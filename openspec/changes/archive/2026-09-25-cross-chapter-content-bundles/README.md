# cross-chapter-content-bundles

One turn may import several chapters: each item declares its chapter, the bundle lands as one batch per chapter (independently rollbackable), and every content block in a reply is applied
