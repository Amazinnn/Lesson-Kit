# Skill: Problem Set Formatting

A problem set should be compact and printable-friendly when requested. Keep answers out. Preserve source numbers and tags. Avoid decorative headings for every problem.

For default compact training sets, use source section headings, bold problem numbers/tags, body-text problem statements, and requested blank lines only in printable versions.
