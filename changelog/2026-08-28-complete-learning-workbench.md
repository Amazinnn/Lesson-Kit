# 2026-08-28 · 学习工作台剩余闭环

完成 `complete-learning-workbench` 变更中的实践范围：练习内容/自评模式分离、结构化选项渲染、目标 CRUD 与计划刷新、Agent 明确练习选题交接、知识列表排序和图谱指标投影。结构关系图仍是默认视图；日历、曲线、插件安装和 AI 自动出题继续保持为后续方向。

验证：目标 Node 测试 58 项通过；知识点/图谱路由测试 35 项通过；完整 `python -m pytest tests -q` 为 274 passed；`node --check` 两个前端脚本通过；`openspec validate complete-learning-workbench --strict` 与 `openspec doctor` 通过。
