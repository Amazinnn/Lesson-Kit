# 时间安排 A 方案（2026-09-02）

- 月历保留传统月视图，黑色重网格改为暖灰细线；今天只高亮日期数字。
- 目标从整块色条改为黄 / 蓝细轨道，起止端点为圆点，跨周末端用箭头续接；
  同一个目标只在首个可见分段显示一次标题，重叠目标继续分轨并列。
- 14 天视图明确命名为「未来 14 天复习负荷」，解释其含义并增加总量、峰值、
  逾期摘要；非零数量放在柱顶，日期固定共线，零值不再伪造底柱。
- 峰值使用黄色强调，达到既有重日阈值时增加红点；「让 Agent 看看」仍然只
  预填重排请求，不发送、不写数据。
- 移除平滑趋势线，避免把同一组准确柱值重复表现成含义不清的第二序列。
