# Reproducible development setup

- Added Python project metadata for the stdlib-only workbench package.
- Added an editable `dev` extra pinned to the CI test runner version.
- Exposed `workbench.cli.main` as the `wb` console command.
- Documented fresh-environment setup and separated repository checks from
  workspace artifact guards.
