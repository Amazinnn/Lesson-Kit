# Lab Artifact Inventory

Use only when lab/system implementation chains cannot be safely audited inside `knowledge-points.md` and `coverage-check.md`.

| artifact_id | source_location | artifact_type | implementation_chain_position | required_operation_or_observation | linked_kp_ids |
|---|---|---|---|---|---|
| lab-001 |  | environment / module / signal / testbench / waveform / report_requirement / verification_step |  |  |  |

## Minimum Completion Standard

This file fails if it lists lab artifacts without implementation-chain position, required operation or observation, and linked `kp_id`s.
