# Discussion Record — lesson-kit 工作台（逐字级保真记录）

> 本文件记录从"项目迁移"到"前端设计"期间与用户的所有讨论成果。
> 保真规则：用户原话直接引用（尽量完整段落；确需省略用 […] 标注）；我的方案只记为"提议"，与用户确认的决策分开标注；**我方设计只要未被用户否定，均作为方案记录（附录 B）**；决策状态：✅已确认 / ❌已否决 / ⏸后置 / ❓待定。
> 记录日期：2026-08-16

---

## 专题 01 · 项目迁移与开工

**背景**：用户要求把 `C:\Users\yanwei\Desktop\Document_In_University\Projects\active\Academic Workflow` 迁移到 `D:\Projects\Academic Workflow` 并完成后续开发。

**用户决策（✅）**：
- 迁移方式：**复制，保留源目录**（"完整复制到 D 盘，源目录原样保留；验证新位置一切正常后你再手动删除。最安全。"）
- 迁移范围：**全部迁移**（lesson-kit + v15/v17 快照 + zip 包 + 工具配置）
- 开发方向：**收尾 dmath ch06 problem-set 视图 + 合并 problem-candidate-mvp 分支**

**用户原话（关于项目状态）**：
> "其实这个项目已经好久没有维护了，现在想一鼓作气做好它。反正也只是趁手工具开发。我现在还不太记得开发进度。"

**执行结果**：全量复制校验通过（8361 文件/98,960,472 字节双向零差异）；FF 合并 feature/problem-candidate-mvp（9 提交）；problem-set guard 实测 PASS；50 测试全绿；提交 `2dff67e` 起逐步推进。

---

## 专题 02 · 宏观定位（grill-me）

**用户提问**：
> "你觉得这个项目在宏观层面还有什么发展空间？这个项目我已经一个月没维护了。"

**我的提问**：这个项目对你到底是什么？A 趁手工具 / B 有哲学内核的个人作品 / C 潜在产品。

**用户决策（✅）**：
> "做成趁手工具吧"

---

## 专题 03 · 学习场景

**我的提问**：你实际想怎么用它？A 考前突击 / B 学期中跟课 / C 日常随手复习。

**用户决策（✅）**：
> "嗯，主要是C，但是考前突击和学习中跟课也非常重要"

**推论**：C 为主（日常复习），A（考前突击）、B（跟课）并重——工具要打通"跟课造材料 → 日常复习 → 考前冲刺"完整回路。

---

## 专题 04 · 工作台形态与核心原则

**用户原话（工作台设想，完整引用）**：
> "现在的问题是，这个项目本来就是为了驱动学习开发的，它不会脱离我具体的、活生生的学习经验，我也不想自己造出来一个虚无缥缈的东西，却不能真实落实到我的学习经历中。
> 我现在粗浅的想法是，干脆直接做一个配套的、用网页渲染的简单工作台。因为我懒得让你开发一个本地的 GUI，所以就直接用浏览器来渲染。
> 具体来说：
> 1. 有一个专门的项目文件夹，还有一个运行时。
> 2. 就像 Claude Code 一样，打开某个文件夹的时候，就单独创建一个运行时的文件夹、配置文件夹。
> 3. 在这里可以管理那些学习的文件。
> 4. 在网页上可以拥有几个工作区，每个文件夹对应一个工作区。
> 5. 在这个工作区里可以看到很多东西。
> 我有点忘记现在的文件是否支持 Obsidian 的视图了，不过我希望它在某种程度上是支持的。简而言之，就是可以像正常阅读 Markdown 文件那样去阅读产生的记录。虽然说肯定还是以阅读那种精简出来的笔记为主，但还是支持那种人类阅读的方式的，这里应该也已经有程序来支持了。
> 我先说几条核心原则：
> 1. 它必须趁手可用。
> 2. 它的逻辑应该足够简单，能够简洁地实现所需要的功能。
> 3. 它应该真实地服务于具体的学习经验。
> 如果你认为这个东西不太现实，或者我认为不太现实，或者是我们都还没讨论过，而且我也给不出具体的学习经验，那你就宁可不要做，或者是只做一个基座，也不要擅自做决定来策划某些功能。"

**确认（✅）**：文件夹 = 工作区；网页一体工作台；Obsidian 式 Markdown 阅读支持；三条核心原则；守护条款（不确定就不做/只做基座，不擅自策划功能）。

---

## 专题 05 · 分层架构确认与工程约束

**用户确认（✅）**：确认五层架构（Shell / Domain / Content / Data / Intelligence 旁挂）+ 两条原则（决策分层：内核决定"教什么"、AI 决定"怎么教"；单向依赖）。

**用户原话（动机与工程约束，完整引用）**：
> "我确认你上面的分层。顺便说一下我的体会：我之前一直想通过苏格拉底式教学和费曼学习法来逼自己掌握知识点，但缺乏一个合格的 Agent 来辅助，这也是我想做这个项目的原因。
> 鉴于我的学习经验尚不成熟，这个项目需要迭代，因此必须保持工整简洁。代码不能冗长，例如不能有乱七八糟的防御性编程，也不能包含哈希值、SHA-256 等。"

**确认（✅）**：工整简洁、禁防御性编程、禁哈希值/SHA-256。

**用户补充（开工要求，✅）**：
> "前面需求你应该已经全部清楚了，功能有什么你大致也清楚了，现在就是把后端的那些全部做好，而且一定要高度模块化！！！高度实现分层设计！"

**用户提供开工检查清单**：`C:\Users\yanwei\Desktop\Knowledge Base\growth\新项目开工检查清单.md`——阶段 0 需求边界 → 阶段 1 架构契约（ARCHITECTURE.md）→ 阶段 2 兼容边界（AGENTS.md）→ 阶段 3 开发纪律 → 阶段 4 新对话初始化 → 阶段 5 验证收尾；"确认前不写业务代码"。

---

## 专题 06 · 学习回路（挠痛处模型）

**用户修正（❌ 否决"先读卡片再答题"的固定流程）**：
> "其实我个人并不想定义时长，虽然肯定也可以，但这么草率地把学习会话钉死不太好。反正我个人的学习原则一直是挠自己的痛处，一直学自己、反复捣鼓自己不会的东西，最后肯定能捣鼓会，而且是以自己最不擅长的姿势来捣鼓它。
> 比如反复过那些薄弱的知识点，而不是直接读知识卡片。如果题目不会，就直接看那道题目；如果计算方法不会，就直接看那个计算过程。我是这个思路，不想规定什么'先读知识卡片，然后再答题，当场看解答'这样把流程钉死，这不是我的学习方式。"

**用户原话（真实学习会话）**：
> "打开工作台，然后进入对应的学科工作区，也就是文件夹对吧？然后它就会列出我哪些知识点是薄弱的。然后我就可以通过 AI 或者自行选择，后端会调用数据库的相关功能，把那些题目给扒拉出来，然后给我过。而且它也是支持那种遗忘曲线功能的，其实。我也会在条件适合的情况下提供反馈。我不希望是那种对着表单一项项打勾的强制反馈，而是既支持用1~5分打分来评估掌握程度，也支持用自然语言反馈薄弱点，系统会标记相关知识点并在下次复习时重点考察。我之前因为没有这样一个庞大且成系统的学习系统辅助复习，导致遇到偏门的计算方法不会做，感到非常沮丧。当然，因为有些知识点必须是有图的，有图的话在展示上就会有一定困难，这个也可以想想办法，看看怎么把知识点比较好地展示出来。
> 你可以去 GitHub 上面找找相关的那种期末应试的项目，就比如说'exam pass assistant'这个项目，好像还是不错的。反正你可以多看看别人是怎么解决真实的学校困难的。"

**确认（✅）**：挠痛处模型（弱项优先、直接练不会的、流程不钉死）；遗忘曲线支持；反馈可选且非表单化（1–5 打分 + 自然语言）；系统标记相关知识点并在下次复习重点考察；图形知识点展示要想办法；调研同类项目（ExamPass Assistant 等）。

---

## 专题 07 · MIT NotebookLM 案例

**用户原话（核心方法，完整引用）**：
> "还有一个可以参考的事例，就是有一个MIT的学霸利用Notebook LM在48小时内通过了考试。你可以查一查他具体的方法，核心步骤包括：
> 1. 核心原则是只把课程大纲、往年真题还有该领域核心概念的经典解释喂进去
> 2. 让 AI 出题而不是让 AI 来总结
> 3. 针对错题反向复习
> 4. 用 AI 来模拟答辩
> 理念就是只为高信噪比的材料，而且先测再学，并且用 AI 模拟被挑战。"

**确认（✅）**：高信噪比输入（大纲/真题/经典解释）；AI 出题而非总结；错题反向复习；AI 模拟答辩（模拟答辩后置，见专题 12 Scoropic）；先测再学。

---

## 专题 08 · AI 形态（转接层 / CLI 数据接口 / 教学流程）

**用户原话（转接层方案，完整引用）**：
> "我的想法是直接做一个转接层，把外部agent的CLI接进来。当然，还可以通过一些特殊操作，确保这些通过文件夹路径区分的agent能够享有共同的记忆。毕竟是一个AI老师嘛，肯定是可以跨对话的，不过这个以后再实现。
> 总之就是给这个工作台做一个套壳，并提供一个独属于这个工作台的超级CLI来操纵相关文件，但Agent本身还是接外部的CLI，我们不提供Harness Engineering。
> 还有一点，我尝试过网页版AI Chat，直接喂讲义让它生成考题，发现题目过于简单，不符合考试规格。后来补充喂了历年真题，生成的题目难度依然不高，且完全模仿原题结构和问法，缺乏归纳、迁移和推广能力。这就是AI Chat的缺点。我并不想做成一个只是可以查看文件的本地聊天，我希望这个项目能够以一些不是对AI进行Harness Engineering，而是优化这个项目的架构来极大的发挥AI能够作为Teacher的潜力。"

**确认（✅）**：转接层接外部 agent CLI；超级 CLI（wb）操纵文件；无 Harness Engineering；架构优于提示词（治 AI Chat 出题缺陷：简单/模仿原题/无归纳迁移）；跨对话记忆后置。

**用户原话（CLI 角色修正）**：
> "A。顺带一提，我认为CLI层面是需要提供一些动作，但它们不能仅仅决定Agent的行为。你感觉你的导向是把CLI想得太强大了，它其实只是提供一个数据接口，让Agent能够生成检查知识以及题目池里的东西，却并不能解释所有的教学行为。其实还需要适配，就是把这个CLI包装进教学Skill里面。简易一点的话可以直接做成Skill，然后通过显示的语言让Agent触发；也可以通过高级一点的方式，比如参考一下DeepTutor是怎么做的，更希望能够通过提示词注入，或者说一些更加系统化的工作流的方式，让Agent能够自如地走一些教学流程。
> 我这里想强调的是，得有一个系统的教学流程，不能仅仅是给一个skill然后让Agent爱理不理就完事了。比如说，不要让它总是就像一个喜欢侃侃而谈的老师：学生发起了一个疑问，老师解答了，然后老师继续追问，学生继续回答，老师继续追问，聊着聊着就偏题了。追问本身并没有错，它追问的动机以及效果本来是好的，但是如果它偏题了，这就是一个很坏的副作用了。"

**确认（✅）**：CLI = 纯数据接口，零教学语义；教学行为在教学 Skill + 契约；要系统化教学流程、防"聊着聊着偏题"。

**用户原话（状态机硬编码的反驳）**：
> "我想要吃点东西，你这里限制得太死了，感觉像硬编码了一样，这也不合理。你不如看看DeepTutor那个项目是如何解决的吧，我不相信它是仅仅依靠Agent自己来解决的。先说一点我的想法：Agent可以自如地借用CLI来探寻题目、探寻对话所需要的材料，但并不绑死一定要用哪种东西。一个会话它可以干很多事情，并不绝对绑定某个任务。只要用户想改，完全可以随时随地再新建一个会话。"

**确认（✅）**：会话弹性（不绑死任务、随时新建会话）；不硬编码教学行为；借鉴 DeepTutor。

**用户原话（OpenSpec 要求与失败教训）**：
> "（还有这个工作区好像还没有OpenSpec这个工作流，所以你可以配置一下）
> 补充一个真实案例：硅谷一位程序员通过跟Claude Code协作，通过了一门考试。这个文件路径是小红书一位博主据此整理的'把AI当老师'的一些提示词，你也可以参考一下。另外补充一下，虽然我这里不提供 Harness，但调用的 CLI 层面那个 Agent 还是需要做一些准备的。这只是一个补充，跟当前的会话进度没有关系。'C:\Users\yanwei\Desktop\中英提示词.pdf'（可以自行调用 /mineru-document-extractor 来解析）
> 我目前的设计都停留在文本和对话层面，没有落实到具体的规范文档。之前几个月尝试与AI协作学习一直失败，估计就是因为没有落实好这些准则。
> 总之当务之急是先把这个已经确认的边界立刻落实到OpenSpec相关工作流的文档里面。你先把我这个文档给编写好。"

**确认（✅）**：OpenSpec 工作流落地；CFP 案例（专题 10）；CLI 层 agent 准备（补充项，后置）。

---

## 专题 09 · DeepTutor 调研（HKUDS）

**调研结论（✅ 采纳）**：
- DeepTutor = "Agent-Native Personalized Learning Assistant"，多智能体协作 + 图增强 RAG + 终身记忆；论文核心立场：硬编码教学行为"brittle and scale poorly"
- 经典 DeepTutor 三环框架：macro（课程规划）/ meso（会话级）/ micro（回合级）适应
- 每次会话结束生成 trace（学习轨迹记录）
- DeepTutor 官方文档：把自己的 CLI 接成 Claude Code subagent——CLI 即数据接口的做法

**设计结果（✅）**：会话 = 记录不是流程管道；弹性会话 + 分层适应 + trace 落 `.lessonkit/jobs/`；防跑题靠锚点 + 停靠 + 用户随时开新会话，不靠强制转移（ADR 0020）。

---

## 专题 10 · CFP-Study 案例与教师行为契约

**材料**：`C:\Users\yanwei\Desktop\中英提示词.pdf`（MinerU token 失效，以 pypdf 提取 618 行英中双语全文）——硅谷程序员 CFP-Study 仓库的 CLAUDE.md。

**内容要点（✅ 吸收为教师行为契约）**：
- 教学理念：耐心学习伙伴、苏格拉底问答法（先问已有知识→基于现有知识→提问引导→拆解→跟进）
- 回复结构四步：初步探索 → 讲解（约 200 字）→ 理解检测（1-2 问）→ 适应性跟进
- 核心行为：提示优先于直接答案、不灌输、不挫败、不讲解底层逻辑就不给答案
- 强制验证协议：考试相关问题严禁猜测——先检索、权威来源、标注来源、歧义如实告知、出错立即承认
- 会话追踪：两步法（每日会话笔记 + 唯一进度追踪文档）——对应 lesson-kit 的池子 + 痕迹

---

## 专题 11 · 判分模型、多元输入与 Scoropic

**用户决策（✅ B 方案 + 补充）**：
> "B，当然也可以提供多元的输入方式，比如说自然语言，或者说如果用户干脆说没时间批改，那就仅仅记录吧。话说是不是我们也需要记录一下对话所产生的作答记录，就像DeepTutor所想的那样。然后其实之后，当然不是现在，之后可以弄一个Scoropic模式，也就是仿照吴乐旻博士提出的'苏格拉底教学法在AI Agent中的应用'的落地产品，通过苏格拉底式追问让学生搞清楚某个东西。当然这个模式仅作为对话模式，现在只提出思想，暂时不考虑怎么落地"

**确认（✅）**：可判分自动判；开放题先作答→揭晓→自评；自然语言输入；"没时间批改"只记录不评分；对话作答记录进 trace（DeepTutor trace-tree 思想）；**Scoropic 苏格拉底对话模式 = 后置**（仅记录思想，ADR 0021）。

---

## 专题 12 · 五科压力测试与 M1–M6 机制

**用户给出的五科场景**（离散数学证明难——"核心知识点就那几条，背都背得出来，但是具体的证明思路非常巧妙"；数电设计——"靠自评加对照参考设计，如果本来自己就水平不够，那直接看参考设计往往不会对自己有多大提升，因为这就类似于抄答案"；教材厚考题不衔接；英语——"是否只考虑了词语本身，而忘记了它们之间的一些记忆连接？……看着中文猜英文，以及看着英文单词猜中文意思，这两者的区别，两者是需要各自的学习动作的"；马原——"论述题、大题以及单选题……辨析、解答、简答、论述题自评，这个自我评价对考生确实是有点要求"；物理——"对真实情景的理解与抽象化，以及范式的转移、归纳和推广……某一步卡住或情境理解不到位，就直接翻车了"；图论/情境题需要图片——"当前系统未必能兼容图片的自如展示"）

**用户总攻点**："你这里走的都是一些条条框框的东西，没有办法进行具体情境具体分析。"

**采纳的机制（✅ M1–M6）**：
- M1 级联信号：方法 KP 连接 + 信号沿 prerequisite/applies_to/part_of 边传导（深度≤2、×0.5/跳、强度加权、证据纯净）
- M2 卡点粒度：solution 分块 + "卡在第 N 步"自然语言标记
- M3 诊断模式：explain 带 user_answer，先定位后提示（治"抄答案"）
- M4 双向回忆卡片：词汇卡 + 方向（英↔中）独立调度 + contrasts/variant_of 邻接提示
- M5 真题衔接：真题覆盖门禁 + 池缺口报告
- M6 图补全：题目也可嵌图（problems.figure_paths）

**用户确认**：
> "好。就按你说的来。顺带一提，我认为这个系统真的要支持那种图床，就是得专门声明一些地方来专门存放图片。"

---

## 专题 13 · 图床与运行时目录原则

**用户决策（✅ A 修正）**：
> "A，但是我想说的一点是，这个程序所有的运行时文件都在这个隐藏文件夹里，就像其他配置文件夹一样，都是以英文句号开头，后面跟一个名字的文件夹。所以这里并不是一个Intermediate，下面再是Course文件夹。"

**确认（✅）**：图床 = `.lessonkit/figures/{course}/{chapter}/`（隐藏点目录原则；非 intermediate 树）；命名 `{owner_id}-fig-{NNN}.png`；池存逻辑路径。

**用户决策（✅ A）**：桥产物归位——`.lessonkit/explain/{course}/{chapter}/{item_id}.md`（跟踪资产）+ `.lessonkit/jobs/<id>/`（gitignored 瞬态）。

---

## 专题 14 · 前端决策点

**用户决策（✅）**：
- 视觉基调（自定义回答）：
> "就按照你所说的极简工具风来，你可以参考一下这个DeepSeek Harness它的前端的这个网页设计，反正就是特别简单就行。还有gpt-taste与UI Pro Max你也可以参考。"
- 首轮范围（自定义回答）：
> "当前好像有一个练习页和知识点展示页，这两个页都可以做出来。我的想法是：左边是选择栏目，中间是具体的页面，最右边是和AI Agent的对话页。"
- UI 语言：**中文界面**（✅）

**确认（✅）**：极简工具风；参考 DSH 前端（后升级为"照搬"，见专题 17）；首轮 = 练习页 + 知识点展示页；**三栏布局：左选择栏 / 中页面 / 右 AI 对话栏**。

---

## 专题 15 · 批量提问的七项回答

**用户决策（✅ 全部）**：
1. AI 上下文绑定：**只跟随不确认**
2. 揭晓节奏（自定义回答）：
> "我觉得可以统一作答完所有题再一次性显示，或者做完一题显示一题答案。而且我觉得，虽然说内容比较多，但界面上还是尽量简洁。如果是反馈，就只简单列一个弹窗，题目下面放一个反馈框就可以了，而且伴随答案出现，这个反馈框才出现。"
3. Agent 深度参与（自定义回答）：
> "然后我认为 Agent 还是要深度参与的，因为那样才方便整理。如果 Agent 暂时断联，那就只记录，不会有任何其他操作。这时候 Agent 可以看到这些做题记录的上下文，知道哪些题做了，哪些题还没有反馈、还没有被细化、还没有根据反馈进行题目入库出库的调整。然后 Agent 就可以根据反馈给这道题目贴标签、归类、放置之类的。"
4. 自评时机：**会话末统一自评**
5. 再练同类（自定义回答）：
> "我觉得不应该跟当前做题流程强绑定，因为它本质上是由当前的薄弱项延展出来的，属于生成题目或练习近似题的步骤，所以可以在会话末了。另外，学习记录也不应该强行绑定某个会话，其实我认为，理想的会话状态就是一个巨无霸的长对话，因为这样子就有那种AI教师一气呵成、从零开始陪伴的感觉。只是说我这样不太现实，但是我也没有更好的方案。。"
6. 服务生命周期：**固定端口 + 前台**
7. 工作区切换：**左栏下拉 + 列表**

**确认（✅）**：反馈框随答案出现（简单弹窗/题下反馈框）；Agent 深度参与（整理/贴标签/归类/放置，断联只记录）；记录不绑会话（池子为真源）；"巨无霸长对话"理想（现实妥协：池子+痕迹实现连续性）。

---

## 专题 16 · AI 全知优先制与极简主义

**用户原话（完整引用）**：
> "你可能还没有太理解，就是里面接入的AI Agent是全知全能的，它上下文其实可以看到所有东西，只是它当前用户看到的题目会被优先列出来，或者说用户最近看到的一些会被优先列出来。
> 这个'优先'你可以自己决定，比如说最近几道题目会作为重点来直接提供给 Agent，不代表它看不到其他已经看过的东西，Agent 都是知道的，只是不会作为明显上下文来直接绑定，Agent 依然可以自如地进行搜索来获知用户都干了什么。
> 还有，目前应该做得简陋一点，不需要弄很多东西。比如说既然有会话末统一自评，那摘要里就没有必要再拼一次。你要知道用户总是希望最简单的，他最讨厌的就是新增很多功能！！！！！！！！"

**确认（✅）**：AI Agent **全知**（可见一切，经 CLI 可搜索所有记录）；"跟随"只是**显示优先级**（当前题/最近题作为明显上下文），不是访问限制；**极简主义**（简陋一点、少功能；摘要不重复拼装；用户最讨厌新增功能）。

---

## 专题 17 · 前端批评与修正（两次）

**第一次批评（用户原话）**：
> "我觉得你连前端都需要展现什么，你自己心里都没数啊。就现在这样的前端，你觉得它有章法吗？你觉得它功能上面的逻辑说得通吗？而且我强调说过，你就仿照DeepSeek Harness的前端UI就可以了，你照搬你不会吗？"

**确认（✅）**：**照搬 DSH 前端 UI**（非参考）；前端要有章法、逻辑说得通。

**第二次批评（用户原话，完整引用）**：
> "不对，AI对话居然是右侧边栏，也就是右边那一栏。但是除此之外的确还有很多设计上的逻辑是说不通的，就比如说不是说左边侧边栏需要展示那个知识图谱嘛，还有那个练习，还有知识点那几个页嘛，这些我们前面讨论的难道你没有记住吗？？？？就是前面的我们讨论，你都需要记住！！！"

**确认（✅）**：**AI 对话 = 右侧边栏**（三栏布局中的右栏）；**左栏侧边栏需要展示：知识图谱、练习、知识点**等页面的导航入口；**前面讨论过的一切都必须记住**。

---

## 专题 18 · 记录要求（本文件缘起）

**用户原话（完整引用）**：
> "我觉得当务之急是，你只把前面我们讨论过的所有成果一字不落地记录下来，而不是凭感觉说我们可能讨论了些什么、重点在哪，然后只实现那些所谓的重点。"

**执行（✅）**：本文件。

---

## 附 · 已落地的产物索引

| 产物 | 内容 |
|---|---|
| `docs/adr/0009–0021` | 分层架构 / 挠痛处调度 / 弹性反馈 / AI 转接层 / 高信噪比输入 / 数据优先视图 / 级联信号 / 诊断模式 / 运行时资产布局 / 真题覆盖门禁 / 学习动作扩展 / Agent-Native 教学 / Scoropic（后置） |
| `openspec/specs/` | review-workbench（14 需求）/ ai-teacher-bridge（10）/ knowledge-figures（5）/ workbench-ui（14）——已归档 |
| `openspec/changes/archive/2026-08-16-workbench-ui/` | 前端变更（2026-08-16 归档，累积规范见 `openspec/specs/workbench-ui/spec.md`） |
| `docs/REQUIREMENTS.md` | 需求 + 边界 + 验收标准 |
| `docs/ARCHITECTURE.md` | 分层模块契约 |
| `AGENTS.md` | 兼容边界 + 开发纪律 + 验证命令 |
| `changelog/` | 2026-08-16 迁移与恢复 / 后端实现记录 |
| 后端实现 | `workbench/`（registry/data/domain/bridge/cli/server），148 测试全绿 |

---

## 附录 B · 我方设计方案汇编（未被否定的方案）

> 规则：以下为我方提出的全部设计方案。凡未被用户否定者，一律记录在案（含具体参数）；✅=用户已确认，📋=我方方案保留（未被否定，亦未被明确确认），⏸=已定后置，❌=已被否决（附原因）。

### B1 架构与工程（✅ 已确认为主）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B1.1 | 五层架构 | Shell（wb CLI+HTTP）→ Domain（纯规则）→ Data（唯一碰 SQLite）；Content 读产物；Intelligence（Bridge）旁挂只被请求；单向依赖禁止反向 | ✅ |
| B1.2 | 决策分层 | 内核决定"教什么"（确定性规则），AI 决定"怎么教"（可插拔）；AI 缺位全链照跑 | ✅ |
| B1.3 | 壳无状态 | 会话/业务状态一律落池，浏览器可随时关 | ✅ |
| B1.4 | 内容数据优先 | 视图直接查池渲染；Markdown 产物降级为打印/导出 | ✅ |
| B1.5 | 工程约束 | stdlib 优先、单进程单端口、无哈希（ID 用可读顺序标识）、增量迁移（pool_schema ensure_* 模式）、禁防御性编程、代码简洁 | ✅ |
| B1.6 | 用户级注册表 | `~/.lessonkit-workbench/workspaces.json`（workspaces 列表：name/path/db/active_course/active_chapter）；`LESSONKIT_WB_HOME` 环境变量可覆盖 | ✅ |
| B1.7 | bridges 配置 | `bridges.json`（provider: command/args/cwd_mode/timeout_s）——JSON 而非 YAML（stdlib 无 YAML 解析） | ✅ |

### B2 数据层（✅ 已确认为主）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B2.1 | review_schedule | PK `(item_type, item_id, direction)`，direction 默认空串（普通项无方向，卡片按方向独立调度）；字段 state/repetitions/ease/interval_days/due_at/last_rating/last_reviewed_at | ✅ |
| B2.2 | feedback_events | 追加日志：id/item_type/item_id/rating/note/created_at | ✅ |
| B2.3 | figure_paths | `knowledge_points.figure_paths` + `problems.figure_paths`（逻辑路径 JSON） | ✅ |
| B2.4 | answer_text | `problem_attempts.answer_text`（开放题作答文本） | ✅ |
| B2.5 | 运行时布局 | `.lessonkit/figures/{course}/{chapter}/`（跟踪）、`.lessonkit/explain/{course}/{chapter}/`（跟踪）、`.lessonkit/jobs/<id>/`（gitignored） | ✅ |

### B3 领域规则（✅ 已确认为主）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B3.1 | 弱项排序 | score = 信号权重 × 到期提升 × 会话内重复惩罚；无信号 0.2 / low 0.5 / medium 1.0 / high 2.0；evidence≥2 ×1.5；到期=1+超期天数、未到期 0.8、无排期 1.0；会话内 ×0.3；只排序不过滤 | ✅ |
| B3.2 | 级联信号 | 沿 prerequisite/applies_to/part_of 反向传导，深度≤2、×0.5/跳、按 relation strength 加权（high 1.0/medium 0.7/low 0.4）；learner_signals 证据纯净（级联不写行）；contrasts/variant_of/generalizes 不参与；UI 显示原因 | ✅ |
| B3.3 | 拉题引擎 | problems（多 KP 命中优先）→ gate_passed 候选 → 缺口报告（永不伪造）；exclude_ids 会话去重；mode weak/random/all；source_kind 过滤 | ✅ |
| B3.4 | 反馈映射 | 1–2→high / 3→medium / 4→low / 5 不清零仅 evidence+1；关键词表（混淆/分不清/区别/搞混→confusion；前置/没学/基础/缺→missing_prerequisite；别的章节/用不上/迁移→transfer_failure；缺联系/关系/连不上→relation_gap；兜底 weak_node）；note 原文保留；problem 反馈传导至关联 KP；信号永不自动清除 | ✅ |
| B3.5 | SM-2 变体 | correct→q4、wrong/stuck→q2、skip→不变；q≥3：reps+1、interval 1/6/×ease、ease+0.1（上限 3.0）；q<3：reps 归零、interval 0、ease−0.2（下限 1.3）、relearning、立即到期；rating 1–5 直接作 q；skip 永不倒退 | ✅ |
| B3.6 | 双向回忆卡片 | memory-recall 型 KP 翻卡（正→回忆→反）；方向（英↔中）独立学习动作、独立调度；contrasts/variant_of 邻接作比较提示（不合并且） | ✅ |
| B3.7 | 卡点粒度 | solution 分块展示；"卡在第 N 步"+自然语言说明落 attempt（note+answer_text）；进 explain/diagnose 上下文 | ✅ |
| B3.8 | 真题覆盖门禁 | 抽取输入含历年真题；`01_inputs/past-paper-coverage.json` 映射真题考点→池内 KP/题目；未映射→FAIL+清单；缺口走候选生成路径 | ✅ |

### B4 桥层（✅ 已确认为主）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B4.1 | 任务协议 | job-XXX 顺序 ID（无哈希）；目录含 task.json（operation/context/output_contract）、task.md（指令）、status.json、stdout.log；状态机 queued→running→done/failed | ✅ |
| B4.2 | provider 执行 | 外部 CLI，cwd=工作区，超时，stdout 落文件（非管道）；非零退出→failed+原因；超时→failed | ✅ |
| B4.3 | 环境变量传参 | `LESSONKIT_JOB_DIR` / `LESSONKIT_OUTPUT_PATH` 传给 provider（输出路径约定） | 📋 |
| B4.4 | 契约校验 | explain 四节（结论/逐步拆解/易错点/回源指向）；diagnose 四节（定位/提示/溯源/追问）；缺节/空节→FAIL+原因；不信任模型输出 | ✅ |
| B4.5 | 教师行为契约 | 先问已知→聚焦简洁讲解（约 200 字）→理解检测→禁猜（对池与源材料校验）→溯源；diagnose 变体：先定位后提示、不给完整答案、结尾追问 | ✅ |
| B4.6 | 会话 trace | 会话记录锚点/交换/学习者作答/结果，落 `.lessonkit/jobs/<conv-id>/`；不锁未来会话；供未来教师记忆 | ✅ |

### B5 CLI / API（✅ 已确认为主）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B5.1 | wb 命令集 | init/ls/open/serve/weak/due/pull/practice/feedback/schedule/ai（explain/diagnose/status）/bridge add/guard——纯数据接口零教学语义 | ✅ |
| B5.2 | HTTP API | hub/weak/due/pull/practice/feedback/problem 详情/kp 详情/figures（越界保护）/ai jobs（创建/状态）/explain 结果读取 | ✅ |
| B5.3 | 服务生命周期 | 固定端口 3081；`wb serve` 前台运行 Ctrl+C 停；`wb open` 打印 URL | ✅ |
| B5.4 | 图床服务 | `/api/w/{name}/figures/{course}/{chapter}/{file}` 静态服务；路径越界 403；缺图 404+UI 占位 | ✅ |

### B6 前端（用户确认 + 我方方案）

| # | 方案 | 具体内容 | 状态 |
|---|---|---|---|
| B6.1 | 三栏布局 | 左=选择栏（工作区下拉+页面导航+弱项列表）、中=页面区、右=**AI 对话栏**（常驻） | ✅（用户强调 AI 对话=右栏） |
| B6.2 | 左栏导航 | 页面入口：**练习 / 知识点 / 知识图谱**；弱项 KP 列表（分数+级联原因） | ✅（用户纠正后） |
| B6.3 | 练习页消息流式 | 题目/作答/揭晓/反馈以消息流呈现（DSH 消息风格）——AI 对话除外（回右栏） | 📋（部分修正：AI 对话移回右栏） |
| B6.4 | 反馈框随答案出现 | 1–5/文本/卡点标记，纯可选；简单弹窗或题下反馈框 | ✅ |
| B6.5 | 会话末统一自评 | 极简：未评题+评分控件+跳过全部+再练同类单按钮；不重复拼装 | ✅ |
| B6.6 | AI 栏上下文 | 只跟随不确认：当前题/最近题为显示优先级；Agent 全知（CLI 可查一切，非访问限制） | ✅ |
| B6.7 | AI 栏操作 | 讲解/诊断一键（带作答文本/卡点）→任务轮询→四节渲染→新会话；无 provider 优雅提示、断联只记录 | ✅ |
| B6.8 | 知识点页 | 正文 md 渲染（LaTeX/wiki 链接跳转/图）、信号与级联原因、关联题、调度状态 | ✅ |
| B6.9 | 知识图谱页 | 复用 `render-graph-html.py` 产物（output/.../ch06-graph.html）或按需生成；v1 只展示不编辑 | ✅ |
| B6.10 | 视觉照搬 DSH | 令牌：字体栈、bg-base #ffffff、文本 #0f1115/#61666b/#81858c、品牌 #0f1115（近黑）、accent 蓝 #4176e6、边框 rgb(0 0 0/4%/.10)、pill 按钮（md 高36圆角18/sm 高28圆角14）、输入高32圆角8、气泡/胶囊圆角22、卡片圆角12、shadow-lv1/2/3、状态色点 | ✅（2026-08-16 修正为 DSH 运行实例实测值，用户确认） |
| B6.11 | 会话状态 | 客户端 sessionStorage（会话内队列）；刷新丢失仅队列，已记录全在池 | 📋 |
| B6.12 | KaTeX 资产 | vendored 至 `workbench/server/static/katex/`（katex.min.js/css/fonts，零构建） | 📋 |
| B6.13 | md 渲染管线 | 先转义再注入 math/wiki/image 标记（顺序敏感，含一次 bug 修复记录） | 📋 |

### B7 后置清单（⏸ 全部后置，未经否定）

generate 桥操作（AI 出题补池缺口，题型学含归纳/迁移/推广）；Scoropic 苏格拉底对话模式；教师记忆消费端；速成模式视图；复习页（前端）；记忆卡片页（前端）；Agent 组织面板（贴标签/归类/候选入出库调整）；批量揭晓；扩展摘要；Obsidian vault 打包；图形资产管理工具；CLI 层 agent 准备（教学 Skill/提示词注入/系统化教学流程）。

## 专题 18：视图重复与合并（2026-08-29）

### 背景

2026-08-29 短暂实现了第 4 导航页「复习」（到期分组列表 + 方向卡会话 +
日历/任务量区块）。所有者指认：复习界面本应对应练习页里的 Yes/No（判断）
功能；时间安排应放在练习页、与学习安排并列。随后做了全库文档漂移审计
（全文：docs/design/2026-08-29-doc-drift-audit.md）。

### 审计结论（引文摘要）

- 「复习页」在 2026-08-29 之前**从未被定义**：仅有三处挂名——B7 后置清单
  （本文件 L368「复习页（前端）」）、2026-08-16-workbench-ui/proposal.md L45
  「Explicitly deferred: review page……」、ARCHITECTURE.md L41 模块注释。
- Yes/No 的归属在全部在先文档中一致：**练习页的模式**（FUTURE-NOTES L80
  「Yes/No 更适合作为 Micro Quiz 的一种回答类型，而不是单独维护一整套
  会话系统」；L313/L322 三选一启动卡；REQUIREMENTS L235/L249）。
- 日历/任务量曲线被记录为「Experimental，前端设计以后再做」
  （FUTURE-NOTES L213），从未指名页面。
- 三页导航是两次在案纠正的决定（本文件 L259 专题 17、L353 B6.2）。
- 裁定：漂移 = 旧文档留白 + 2026-08-29 两个提案（review-page、
  calendar-workload）用自行解读填充空白并经 strict 校验固化。

### 决定（方案一）

1. 导航回三页；复习页移除。
2. 到期提醒回归练习页：与今日计划队列合流为一张「今天」列表，行内人话
   原因（覆盖仍低 / 拖了 N 天 / 上次没记住），零调度参数。
3. 时间安排（月历 + 14 天柱状图）移至练习页，与学习安排并列。
4. 方向卡片会话 = 闪卡/判断模式内、范围含到期方向行时的可选轻会话
   （ADR 0019 offered, never required）。
5. 保留 additive API：pull include_ids、feedback direction、/calendar、
   micro quiz 契约与判分。

变更提案：openspec/changes/consolidate-practice-views/（由实现会话执行）。

## 专题 19：准备练习列表与候选按需（2026-08-29，实现会话改判）

### 背景

专题 18 方案一原定「计划 ∪ 到期自动合流为一张今天列表 + 模式内方向卡轻会话」。
实现会话开工讨论时所有者重新表述意图，推翻其中两条，openspec 变更文档随之改写。

### 决定

1. 练习之前，无论来自计划还是到期，都不是真正要练的；**只有用户显式选定的
   才算**。练习页唯一「要练的」清单 =「准备练习」列表 = 当前知识点选区
   （`wb_kp_selection_{ws}`）的行视图：知识点名 + 清除，双向同步，零新存储。
2. 计划与到期降为**按需候选**：「加今天要练的（N）」按钮（带计数）点开才
   出现，行 = 知识点名 · 一个短语 · 加入；「今天先做」计划卡片解散，计划
   输出成为候选来源之一，重算按钮保留；到期不做常驻视图，
   `queries.review_overview` 逻辑保留作候选源与每日规划参考。
3. **字段极简硬约束**：候选/准备行不带徽标、日期、数字、第二短语——所有者
   原话大意：哪怕工整的字段也会大大加重阅读量，能少则少。
4. 方向卡 UI 全拆：不需要系统导向会话，会话由用户自开；真实池中尚无方向
   调度行（功能等于零）。调度键与 `feedback.direction` API 保留，卡片 UI
   待真实使用再议。
5. UI 只到知识点级：到期题目行不进任何 UI；`pull.include_ids` API 保留、
   暂无 UI 消费者。
6. 空态一句话一动作；候选入口按钮带计数；顺手只修主按钮禁用态样式，其余
   UX 遗留（图谱标签重叠、会话不聚焦、40 字截断无省略号）另办；
   交付拆 3 个 PR（文档 / 实现 / 走查归档）。

### 与专题 18 的关系

专题 18 的第 1（三页导航）、3（时间安排练习页并列）、5（保留 additive API）
条不变；第 2 条（自动合流今天列表）被本专题第 1–3 条取代；第 4 条（卡片轻
会话）被本专题第 4 条取代。

## 专题 20：practice-loop-ux-completion 与 generate 桥定义（2026-08-29）

所有者经 grilling 拍板（本阶段操作模型）：

1. **batch 模式新语义**：判分即时（micro/yes_no 横幅 + 答错高亮正确项，
   停留约 2 秒自动翻页），自评延迟（收束页统一）。「延迟反馈」原设计推翻，
   batch 的价值 = 不打断节奏。
2. **闪卡会话**：历史回翻 + 前进拉新（状态保留、末尾拉新、结束路径不变）；
   由会话牌组（practice-deck.js）承载，横幅与讲解/诊断上下文共用该层。
3. **标签一律完整显示**：不截断、不省略号；名称过长属命名纪律，UI 不兜底。
   图谱标签重叠用「标签盒确定性避让」解决（不动投影管线）。
4. **讲解/诊断前端补齐**：spec 既有 "shown in the UI" 的履约；按钮门槛以
   bridges.json 任务 provider 为准（新增 GET /ai/task-providers）。
5. **generate 桥定稿**（三要素见 PENDING-DEFINITIONS）：AI 产出 manifest →
   所有者触发既有门禁入池；无逐题人工确认；批次 id + 整批回滚为安全网；
   题型学（归纳/迁移/推广）为桥上显式未定项；实现另立项。
6. 交付：一个 OpenSpec change（PR #24 规格 + PR #25 实现）+ 手册 8–9 章
   成文 + generate 桥定义（本专题与 GLOSSARY/PENDING 同批入库）。

## 专题 21：讲解/诊断退役与动作面定调（2026-08-29 问卷）

所有者经动作面问卷（docs/superpowers/plans/2026-08-29-agent-action-questionnaire.md）定案：

1. **讲解/诊断彻底移除**（A1）：前端按钮 + API + spec 条款全删，
   `.lessonkit/explain/` 产物随清（A3）。理由：Agent 上下文本就是整个版面，
   按题特化是多余工作——想聊某道题直接在对话里说。
2. **Agent 写权限全开**（B1）：记录类/整理类/入池触发都放行；「门禁步骤是
   决断辅助，不是权限闸门」。此口径推翻 generate 桥定稿中的「所有者触发」。
3. **组织面板并入管线**（C）：候选贴标签/归类/出入库不是独立动作，而是
   生成→校验→入库管线的校验环节；管线或更名 Check（立项时定名）。
4. **无候选中间态**：生成并校验通过即直接入正式池，安全网是批次溯源+
   整批回滚；candidate_problems 表去留随 Check 立项定。
5. **触发形态**（D1）：只走对话内自然语言→结构化动作；**显式命令面板
   严格杜绝**；CLI 触发保留可能性。呈现分型（D2）：独立结果进对话流，
   融入型结果（如 Agent 填目标字段）版面原位变化。
6. 落地：remove-explain-diagnose 变更（任务机五模块+四路由+CLI ai 退役，
   对话桥与 bridge add 保留）；队列 ③目标补齐 ④Check 立项 排队。

## 专题 22：Check 管线立项定名与口径（2026-08-29 立项 grilling）

队列④开工。所有者经开场 grilling 五问定案（问题清单见
docs/superpowers/plans/2026-08-29-check-pipeline-handoff.md 第三节）：

1. **定名 Check**：采纳「Check 管线」，替代「generate 桥」。理由：强调
   校验准入（生成→校验→直接入池），且无候选中间态后「桥」已名不副实。
   PENDING-DEFINITIONS「generate 桥操作」条目迁往 GLOSSARY「Check 管线」。
2. **回滚命令形态**：`wb ingest rollback --batch <id>`，挂在 ingest 子命令
   族（「ingest 是内容治理唯一写池通道」口径不变）；回滚前自动做安全
   备份，回滚后输出 accounting 核对。
3. **首期 manifest 产出链**：只立闪卡+微题（两种门禁配方现成、真实池
   验证过）；综合题（大题）AI 出题配方为显式后续项（问卷 C 组已勾意愿，
   首期不落地，留 spec 未来段）。
4. **candidate_problems 退役**：pull/mastery/hub 停止读取；表与 `wb data
   candidate` 子命令保留但标「待退役」，不 DROP（additive 纪律）；将来
   抽取管线入池统一走 Check 门禁，不再有中间态。「Agent 组织面板」挂名
   条目随此正式了结（被 Check 校验环节吸收，专题 21 第 3 条）。
5. **产出入口 = 对话桥结构化动作**：对话内自然语言 → `check_ingest`
   结构化动作（manifest 内联）→ 服务端过既有确定性门禁 → apply →
   独立结果卡片进对话流（批次 id、计数、备份路径、回滚按钮）；门禁
   失败逐条显式呈现，不静默丢弃。外部 Agent 会话写文件跑 CLI 的通道
   照旧可用（问卷 B1 口径不变）。

**cloze 澄清**（所有者在问卷 C 组批注：「没有Cloze这个题目类型吧，这个你
需要跟我解释清楚它是什么时候冒出来的」）：cloze **不是微题题型**——微题
题型只有 yes_no / single_choice / multiple_choice 三种（retired 类型一律
拒收）。它是**闪卡侧**的「挖空成卡」形态：知识点拆键值对时，把答案从
表述里挖空生成填空卡（front 完整表述、back 挖空），属闪卡自动解构的
产物形态之一。何时冒出来的：introduce-flash-card 变更时闪卡 spec 未来段
「cloze-style automatic card derivation」首次入册，专题 20 generate 桥
定稿注记「闪卡 AI 自动解构（含 cloze 挖空批量成卡）」沿用。本问卷未
勾选，不入 Check 首期范围，留闪卡 spec 未来段。

交付：OpenSpec change `introduce-check-pipeline`（规格 → 实现 → 走查 →
归档）；本专题五答为该变更 proposal Why 的事实来源。

## 专题 23：Check Agent 产出链完整交付定标（2026-08-30 问卷）

introduce-check-pipeline 交付后，所有者反馈「什么都实现了一点，但好像又没
实现完全」，经拷问定性为：**真 Agent 出题链没走通**（演示出题来自 stub 脚本；
真实 provider 未验证；门禁结果不回给 Agent）。所有者要求按《新项目开工检查
清单》（Desktop Knowledge Base/growth/）交付**完整的开发结果**，指定
to-questionnaire 定范围。问卷（docs/superpowers/plans/2026-08-30-check-
agent-chain-questionnaire.md）四答定案：

1. **范围**：真 Agent 产出链+benchmark，以及除综合题出题以外的所有面向
   Agent 的 CLI；agent 产出的题/卡自带 topic_label 等完备格式，由门禁校验。
   不做：界面闭环、综合题配方、候选表 DROP。
2. **benchmark 规模**：codex 单链路 10 轮×2 kind=20 轮（hold-out KP）。
3. **成功判定（严格）**：未经修正首轮合规入库才算成功，每种 kind 首轮
   成功率 ≥60% 为达标线；修正后最终成功率另行报告。
4. **真实池验收**：benchmark 达标后由所有者亲自在真实工作区实验（最终验收）。

工作法：codex sub-agent 全程 ≤2 并行、文件集不交叉（问卷 Anythin-else 原话
「依然鼓励你使用并行不多于2个Codex作为Sub Agent」）。开工清单的 benchmark
验收机制（验收陈述/hold-out 隔离/反向用例/真实回放/成本报告/退役条件）在本轮
首次落地，定稿于 harden-check-agent-chain 的 design.md。

交付：OpenSpec change `harden-check-agent-chain`。

## 专题 24：加固批次评审、出题链验收修复与候选物理退役（2026-08-30）

**一、14 分支加固批次（#39–#52）评审合并。** 远端出现所有者侧批量产生的
14 个开放 PR（两波创建、单 commit、自称"14 分支集成 372 项测试"）。逐个
diff 评审 + 本地按合并序积分跑基线后按序 rebase-merge，全部零冲突。评审
确认非过度防御：坏 JSON/坏参数当时会**无 HTTP 响应断连**（#41/#49 是真修
复）；`kp_ids` 全库规范 JSON（#39 收紧安全）；前端全部 POST 自带
Content-Type（415 门无伤）；#52 回滚加锁比 spec 更严谨；#43 将
next_free_ids 语义改为章节前缀范围（Check 链兼容）。#45 起 CI 强制
openspec strict + compileall + 双 guard。

**二、所有者验收第一击：出题链断裂（conv-023）。** 真实 provider（Claude）
下说「给知识点补两张闪卡」：回复带了动作区块但无结果卡片、零写入，Agent
在正文里两轮虚构「已写入」。回放定位三缺陷并修复（openspec:
disclose-ignored-action-blocks）：解析器只认第一个动作区块（多区块时
manifest 被遮蔽）；check_intent 正则不认「补两张闪卡」；忽略无声使幻觉申报
成为可能。修复后：全区块按意图匹配、自然措辞可触发、被忽略区块向下一轮
上下文披露「未写入任何内容」。

**三、候选机制物理退役（remove-candidate-store）。** 专题 22 定的「待退役」
观察期结束、无任何消费者，所有者点名执行：`wb data` candidate 实体与
gate/promote 动作下线、候选证据分支删除、两表停止创建且真实池 DROP（先整
池备份）。learner_signals 确认为核心保留（codex lane 曾误将其兼容入口掏空，
已修）。ADR 0008 标 Superseded。执行插曲：codex sub-agent 中途耗尽用量限额，
按「核对残留再接手」纪律人工收尾。

**四、方向拷问（grill-me）。** 逐分支定案：①验收先行（重启 3081 走四步）；
②综合题配方 × 真题拟合**联合立项**、等 Check 首期一轮真实使用后启动（依据：
8-27 反对 AI 大题三理由中幻觉已被门禁治理，难度/结构正是拟合所供；语料已
入池 source_kind 区分；ADR 0018 目标=归纳/迁移/推广变体）；③轻量收尾窗口
= 界面闭环（视验收）/codex 延迟治理（视验收）/候选 DROP（本轮已做）；cloze
不进窗口；④教师记忆消费端保持机会主义（考证：单条目史可达、语料级不可达、
零自动注入）；⑤难度评价需跨题型统一体系，具体算法排前端设计之后（模块化
可插拔）。新挂名登记：「真题拟合」（并入联合立项）、「双向闪卡」（语言学习
形态）。全部留痕于 FUTURE-DEVELOPMENT-NOTES「方向拷问结论」与
PENDING-DEFINITIONS。

## 专题 25：前端探索与目标时间跑道（2026-08-31）

所有者停止常规问题审计，要求前端转向柔和蒙德里安风格，并优先补齐两个具体体验：

1. 时间安排里的长期/阶段目标不再只按截止日落格，而是从开始日到截止日绘制为横向跑道；重叠目标上下并列。
2. 目标新增可选开始日期；旧目标不迁移，按截止日显示为单日跑道。跨周目标按自然周切段但保持同一目标身份。
3. 跑道采用有限色彩语义：阶段目标黄色、长期目标蓝色、逾期红色边缘；月历保留黑色结构线与暖白底，先局部验证蒙德里安语言，不顺手翻新全站。
4. 视图继续只读；不发展为甘特项目管理器，不自动排期。14 天任务量与重日 Agent 预填保持原行为。
5. 后续独立 PR 将把 Agent 工具/CLI/流式事件从原始状态码升级为对话内执行计划；练习页切换导致右栏退出的问题暂不处理。
## 专题 25：前端设计主线与图谱交互（2026-09-01）

所有者确认下一阶段以柔和蒙德里安为主体视觉，怀旧终端感只服务于 Agent
命令、工具输出和代码。全站先建立语义颜色、字体防溢出、几何和动效契约，再
逐项实现，最后统一构图；知识图谱承担主要空间动画，普通阅读与练习页面保持
安静。

图谱默认关系结构不变。投影指标可同时控制节点面积、距所属中心的距离和带图例
的色谱；切换采用错峰、带切向滑动的柔和重排，稳定后完全静止。关系视图继续
使用确定性候选布局并增加有限交叉优化；关系强度控制管线深浅和宽度，阴影、
管体与高光形成有限立体感，强关系后绘制并覆盖弱关系。筛选与投影相互独立，
支持状态多选和多软中心分团，跨团真实关系保留但弱化。

状态字段固定为 `needs_work / review / mastered / null`，不新增完成度或进度状态；
空值界面文案为「未标记」，不得解释成「未开始」。状态投影表达关注优先级：
`needs_work` 最靠中心，依次为 `review`、空值、`mastered`。后续实现还包括中栏
右上角可折叠练习范围托盘、练习阶段单一主动作、数字填空加备注的双行紧凑
自评、Agent 会话跨页连续性，以及不依赖颜色的状态轮廓。自评选择不改为五个
按钮；学生直接输入 1–5，再显式提交「记录并下一题」。
## 专题 26：Agent 执行计划视图（2026-08-31）

所有者要求 Agent 对话像 Codex 应用：每次工具调用、CLI 与流式输出在对话框里
按计划逐条显示，不再用一长串大写英文状态刷新。实现口径定为：Bridge 将 Codex
与 Claude 的稳定事件归一成命令/工具/搜索/回答四类活动；同一 provider item 的
开始与完成更新同一行；状态只显示中文「进行中 / 已完成 / 失败」；命令与工具
输出放入可展开区域，长文本在右栏内部换行或滚动。普通回答仍是独立流式气泡。

执行计划不等于思维链：只展示 provider 明确给出的外部活动，reasoning 只显示
「分析任务」而不保存或展示正文。成功轮次保存归一后的活动记录，重新打开会话
可恢复；失败与取消仍不写成功 transcript。练习页进出导致右栏显示变化继续按
所有者要求留在本轮 scope 之外。
