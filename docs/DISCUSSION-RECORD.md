# Discussion Record — lesson-kit 工作台（逐字级保真记录）

> 本文件记录从"项目迁移"到"前端设计"期间与用户的所有讨论成果。
> 保真规则：用户原话直接引用（尽量完整段落；确需省略用 […] 标注）；我的方案只记为"提议"，与用户确认的决策分开标注；决策状态：✅已确认 / ❌已否决 / ⏸后置 / ❓待定。
> 记录日期：2026-08-16

---

## 专题 01 · 项目迁移与开工

**背景**：用户要求把 `C:\Users\yanwei\Desktop\Document_In_University\Projects\active\Academic Workflow` 迁移到 `D:\Projects\Academic Workflow` 并完成后续开发。

**用户决策（✅）**：
- 迁移方式：**复制，保留源目录**（"完整复制到 D 盘，源目录原样保留；验证新位置一切正常后你再手动删除。最安全。"）
- 迁移范围：**全部迁移**（lesson-kit + v15/v17 快照 + zip 包 + 工具配置）
- 开发方向：**收尾 dmath ch06 problem-set 视图 + 合并 problem-candidate-mvp 分支**

**用户原话（关于项目状态）**：
> "其实这个项目已经好久没有维护了，现在想一鼓作气做好它。反正也只是趁手工具开发。我现在还不太记得开发进度。"

**执行结果**：全量复制校验通过（8361 文件/98,960,472 字节双向零差异）；FF 合并 feature/problem-candidate-mvp（9 提交）；problem-set guard 实测 PASS；50 测试全绿；提交 `2dff67e` 起逐步推进。

---

## 专题 02 · 宏观定位（grill-me）

**用户提问**：
> "你觉得这个项目在宏观层面还有什么发展空间？这个项目我已经一个月没维护了。"

**我的提问**：这个项目对你到底是什么？A 趁手工具 / B 有哲学内核的个人作品 / C 潜在产品。

**用户决策（✅）**：
> "做成趁手工具吧"

---

## 专题 03 · 学习场景

**我的提问**：你实际想怎么用它？A 考前突击 / B 学期中跟课 / C 日常随手复习。

**用户决策（✅）**：
> "嗯，主要是C，但是考前突击和学习中跟课也非常重要"

**推论**：C 为主（日常复习），A（考前突击）、B（跟课）并重——工具要打通"跟课造材料 → 日常复习 → 考前冲刺"完整回路。

---

## 专题 04 · 工作台形态与核心原则

**用户原话（工作台设想，完整引用）**：
> "现在的问题是，这个项目本来就是为了驱动学习开发的，它不会脱离我具体的、活生生的学习经验，我也不想自己造出来一个虚无缥缈的东西，却不能真实落实到我的学习经历中。
> 我现在粗浅的想法是，干脆直接做一个配套的、用网页渲染的简单工作台。因为我懒得让你开发一个本地的 GUI，所以就直接用浏览器来渲染。
> 具体来说：
> 1. 有一个专门的项目文件夹，还有一个运行时。
> 2. 就像 Claude Code 一样，打开某个文件夹的时候，就单独创建一个运行时的文件夹、配置文件夹。
> 3. 在这里可以管理那些学习的文件。
> 4. 在网页上可以拥有几个工作区，每个文件夹对应一个工作区。
> 5. 在这个工作区里可以看到很多东西。
> 我有点忘记现在的文件是否支持 Obsidian 的视图了，不过我希望它在某种程度上是支持的。简而言之，就是可以像正常阅读 Markdown 文件那样去阅读产生的记录。虽然说肯定还是以阅读那种精简出来的笔记为主，但还是支持那种人类阅读的方式的，这里应该也已经有程序来支持了。
> 我先说几条核心原则：
> 1. 它必须趁手可用。
> 2. 它的逻辑应该足够简单，能够简洁地实现所需要的功能。
> 3. 它应该真实地服务于具体的学习经验。
> 如果你认为这个东西不太现实，或者我认为不太现实，或者是我们都还没讨论过，而且我也给不出具体的学习经验，那你就宁可不要做，或者是只做一个基座，也不要擅自做决定来策划某些功能。"

**确认（✅）**：文件夹 = 工作区；网页一体工作台；Obsidian 式 Markdown 阅读支持；三条核心原则；守护条款（不确定就不做/只做基座，不擅自策划功能）。

---

## 专题 05 · 分层架构确认与工程约束

**用户确认（✅）**：确认五层架构（Shell / Domain / Content / Data / Intelligence 旁挂）+ 两条原则（决策分层：内核决定"教什么"、AI 决定"怎么教"；单向依赖）。

**用户原话（动机与工程约束，完整引用）**：
> "我确认你上面的分层。顺便说一下我的体会：我之前一直想通过苏格拉底式教学和费曼学习法来逼自己掌握知识点，但缺乏一个合格的 Agent 来辅助，这也是我想做这个项目的原因。
> 鉴于我的学习经验尚不成熟，这个项目需要迭代，因此必须保持工整简洁。代码不能冗长，例如不能有乱七八糟的防御性编程，也不能包含哈希值、SHA-256 等。"

**确认（✅）**：工整简洁、禁防御性编程、禁哈希值/SHA-256。

**用户补充（开工要求，✅）**：
> "前面需求你应该已经全部清楚了，功能有什么你大致也清楚了，现在就是把后端的那些全部做好，而且一定要高度模块化！！！高度实现分层设计！"

**用户提供开工检查清单**：`C:\Users\yanwei\Desktop\Knowledge Base\growth\新项目开工检查清单.md`——阶段 0 需求边界 → 阶段 1 架构契约（ARCHITECTURE.md）→ 阶段 2 兼容边界（AGENTS.md）→ 阶段 3 开发纪律 → 阶段 4 新对话初始化 → 阶段 5 验证收尾；"确认前不写业务代码"。

---

## 专题 06 · 学习回路（挠痛处模型）

**用户修正（❌ 否决"先读卡片再答题"的固定流程）**：
> "其实我个人并不想定义时长，虽然肯定也可以，但这么草率地把学习会话钉死不太好。反正我个人的学习原则一直是挠自己的痛处，一直学自己、反复捣鼓自己不会的东西，最后肯定能捣鼓会，而且是以自己最不擅长的姿势来捣鼓它。
> 比如反复过那些薄弱的知识点，而不是直接读知识卡片。如果题目不会，就直接看那道题目；如果计算方法不会，就直接看那个计算过程。我是这个思路，不想规定什么'先读知识卡片，然后再答题，当场看解答'这样把流程钉死，这不是我的学习方式。"

**用户原话（真实学习会话）**：
> "打开工作台，然后进入对应的学科工作区，也就是文件夹对吧？然后它就会列出我哪些知识点是薄弱的。然后我就可以通过 AI 或者自行选择，后端会调用数据库的相关功能，把那些题目给扒拉出来，然后给我过。而且它也是支持那种遗忘曲线功能的，其实。我也会在条件适合的情况下提供反馈。我不希望是那种对着表单一项项打勾的强制反馈，而是既支持用1~5分打分来评估掌握程度，也支持用自然语言反馈薄弱点，系统会标记相关知识点并在下次复习时重点考察。我之前因为没有这样一个庞大且成系统的学习系统辅助复习，导致遇到偏门的计算方法不会做，感到非常沮丧。当然，因为有些知识点必须是有图的，有图的话在展示上就会有一定困难，这个也可以想想办法，看看怎么把知识点比较好地展示出来。
> 你可以去 GitHub 上面找找相关的那种期末应试的项目，就比如说'exam pass assistant'这个项目，好像还是不错的。反正你可以多看看别人是怎么解决真实的学校困难的。"

**确认（✅）**：挠痛处模型（弱项优先、直接练不会的、流程不钉死）；遗忘曲线支持；反馈可选且非表单化（1–5 打分 + 自然语言）；系统标记相关知识点并在下次复习重点考察；图形知识点展示要想办法；调研同类项目（ExamPass Assistant 等）。

---

## 专题 07 · MIT NotebookLM 案例

**用户原话（核心方法，完整引用）**：
> "还有一个可以参考的事例，就是有一个MIT的学霸利用Notebook LM在48小时内通过了考试。你可以查一查他具体的方法，核心步骤包括：
> 1. 核心原则是只把课程大纲、往年真题还有该领域核心概念的经典解释喂进去
> 2. 让 AI 出题而不是让 AI 来总结
> 3. 针对错题反向复习
> 4. 用 AI 来模拟答辩
> 理念就是只为高信噪比的材料，而且先测再学，并且用 AI 模拟被挑战。"

**确认（✅）**：高信噪比输入（大纲/真题/经典解释）；AI 出题而非总结；错题反向复习；AI 模拟答辩（模拟答辩后置，见专题 12 Scoropic）；先测再学。

---

## 专题 08 · AI 形态（转接层 / CLI 数据接口 / 教学流程）

**用户原话（转接层方案，完整引用）**：
> "我的想法是直接做一个转接层，把外部agent的CLI接进来。当然，还可以通过一些特殊操作，确保这些通过文件夹路径区分的agent能够享有共同的记忆。毕竟是一个AI老师嘛，肯定是可以跨对话的，不过这个以后再实现。
> 总之就是给这个工作台做一个套壳，并提供一个独属于这个工作台的超级CLI来操纵相关文件，但Agent本身还是接外部的CLI，我们不提供Harness Engineering。
> 还有一点，我尝试过网页版AI Chat，直接喂讲义让它生成考题，发现题目过于简单，不符合考试规格。后来补充喂了历年真题，生成的题目难度依然不高，且完全模仿原题结构和问法，缺乏归纳、迁移和推广能力。这就是AI Chat的缺点。我并不想做成一个只是可以查看文件的本地聊天，我希望这个项目能够以一些不是对AI进行Harness Engineering，而是优化这个项目的架构来极大的发挥AI能够作为Teacher的潜力。"

**确认（✅）**：转接层接外部 agent CLI；超级 CLI（wb）操纵文件；无 Harness Engineering；架构优于提示词（治 AI Chat 出题缺陷：简单/模仿原题/无归纳迁移）；跨对话记忆后置。

**用户原话（CLI 角色修正）**：
> "A。顺带一提，我认为CLI层面是需要提供一些动作，但它们不能仅仅决定Agent的行为。你感觉你的导向是把CLI想得太强大了，它其实只是提供一个数据接口，让Agent能够生成检查知识以及题目池里的东西，却并不能解释所有的教学行为。其实还需要适配，就是把这个CLI包装进教学Skill里面。简易一点的话可以直接做成Skill，然后通过显示的语言让Agent触发；也可以通过高级一点的方式，比如参考一下DeepTutor是怎么做的，更希望能够通过提示词注入，或者说一些更加系统化的工作流的方式，让Agent能够自如地走一些教学流程。
> 我这里想强调的是，得有一个系统的教学流程，不能仅仅是给一个skill然后让Agent爱理不理就完事了。比如说，不要让它总是就像一个喜欢侃侃而谈的老师：学生发起了一个疑问，老师解答了，然后老师继续追问，学生继续回答，老师继续追问，聊着聊着就偏题了。追问本身并没有错，它追问的动机以及效果本来是好的，但是如果它偏题了，这就是一个很坏的副作用了。"

**确认（✅）**：CLI = 纯数据接口，零教学语义；教学行为在教学 Skill + 契约；要系统化教学流程、防"聊着聊着偏题"。

**用户原话（状态机硬编码的反驳）**：
> "我想要吃点东西，你这里限制得太死了，感觉像硬编码了一样，这也不合理。你不如看看DeepTutor那个项目是如何解决的吧，我不相信它是仅仅依靠Agent自己来解决的。先说一点我的想法：Agent可以自如地借用CLI来探寻题目、探寻对话所需要的材料，但并不绑死一定要用哪种东西。一个会话它可以干很多事情，并不绝对绑定某个任务。只要用户想改，完全可以随时随地再新建一个会话。"

**确认（✅）**：会话弹性（不绑死任务、随时新建会话）；不硬编码教学行为；借鉴 DeepTutor。

**用户原话（OpenSpec 要求与失败教训）**：
> "（还有这个工作区好像还没有OpenSpec这个工作流，所以你可以配置一下）
> 补充一个真实案例：硅谷一位程序员通过跟Claude Code协作，通过了一门考试。这个文件路径是小红书一位博主据此整理的'把AI当老师'的一些提示词，你也可以参考一下。另外补充一下，虽然我这里不提供 Harness，但调用的 CLI 层面那个 Agent 还是需要做一些准备的。这只是一个补充，跟当前的会话进度没有关系。'C:\Users\yanwei\Desktop\中英提示词.pdf'（可以自行调用 /mineru-document-extractor 来解析）
> 我目前的设计都停留在文本和对话层面，没有落实到具体的规范文档。之前几个月尝试与AI协作学习一直失败，估计就是因为没有落实好这些准则。
> 总之当务之急是先把这个已经确认的边界立刻落实到OpenSpec相关工作流的文档里面。你先把我这个文档给编写好。"

**确认（✅）**：OpenSpec 工作流落地；CFP 案例（专题 10）；CLI 层 agent 准备（补充项，后置）。

---

## 专题 09 · DeepTutor 调研（HKUDS）

**调研结论（✅ 采纳）**：
- DeepTutor = "Agent-Native Personalized Learning Assistant"，多智能体协作 + 图增强 RAG + 终身记忆；论文核心立场：硬编码教学行为"brittle and scale poorly"
- 经典 DeepTutor 三环框架：macro（课程规划）/ meso（会话级）/ micro（回合级）适应
- 每次会话结束生成 trace（学习轨迹记录）
- DeepTutor 官方文档：把自己的 CLI 接成 Claude Code subagent——CLI 即数据接口的做法

**设计结果（✅）**：会话 = 记录不是流程管道；弹性会话 + 分层适应 + trace 落 `.lessonkit/jobs/`；防跑题靠锚点 + 停靠 + 用户随时开新会话，不靠强制转移（ADR 0020）。

---

## 专题 10 · CFP-Study 案例与教师行为契约

**材料**：`C:\Users\yanwei\Desktop\中英提示词.pdf`（MinerU token 失效，以 pypdf 提取 618 行英中双语全文）——硅谷程序员 CFP-Study 仓库的 CLAUDE.md。

**内容要点（✅ 吸收为教师行为契约）**：
- 教学理念：耐心学习伙伴、苏格拉底问答法（先问已有知识→基于现有知识→提问引导→拆解→跟进）
- 回复结构四步：初步探索 → 讲解（约 200 字）→ 理解检测（1-2 问）→ 适应性跟进
- 核心行为：提示优先于直接答案、不灌输、不挫败、不讲解底层逻辑就不给答案
- 强制验证协议：考试相关问题严禁猜测——先检索、权威来源、标注来源、歧义如实告知、出错立即承认
- 会话追踪：两步法（每日会话笔记 + 唯一进度追踪文档）——对应 lesson-kit 的池子 + 痕迹

---

## 专题 11 · 判分模型、多元输入与 Scoropic

**用户决策（✅ B 方案 + 补充）**：
> "B，当然也可以提供多元的输入方式，比如说自然语言，或者说如果用户干脆说没时间批改，那就仅仅记录吧。话说是不是我们也需要记录一下对话所产生的作答记录，就像DeepTutor所想的那样。然后其实之后，当然不是现在，之后可以弄一个Scoropic模式，也就是仿照吴乐旻博士提出的'苏格拉底教学法在AI Agent中的应用'的落地产品，通过苏格拉底式追问让学生搞清楚某个东西。当然这个模式仅作为对话模式，现在只提出思想，暂时不考虑怎么落地"

**确认（✅）**：可判分自动判；开放题先作答→揭晓→自评；自然语言输入；"没时间批改"只记录不评分；对话作答记录进 trace（DeepTutor trace-tree 思想）；**Scoropic 苏格拉底对话模式 = 后置**（仅记录思想，ADR 0021）。

---

## 专题 12 · 五科压力测试与 M1–M6 机制

**用户给出的五科场景**（离散数学证明难——"核心知识点就那几条，背都背得出来，但是具体的证明思路非常巧妙"；数电设计——"靠自评加对照参考设计，如果本来自己就水平不够，那直接看参考设计往往不会对自己有多大提升，因为这就类似于抄答案"；教材厚考题不衔接；英语——"是否只考虑了词语本身，而忘记了它们之间的一些记忆连接？……看着中文猜英文，以及看着英文单词猜中文意思，这两者的区别，两者是需要各自的学习动作的"；马原——"论述题、大题以及单选题……辨析、解答、简答、论述题自评，这个自我评价对考生确实是有点要求"；物理——"对真实情景的理解与抽象化，以及范式的转移、归纳和推广……某一步卡住或情境理解不到位，就直接翻车了"；图论/情境题需要图片——"当前系统未必能兼容图片的自如展示"）

**用户总攻点**："你这里走的都是一些条条框框的东西，没有办法进行具体情境具体分析。"

**采纳的机制（✅ M1–M6）**：
- M1 级联信号：方法 KP 连接 + 信号沿 prerequisite/applies_to/part_of 边传导（深度≤2、×0.5/跳、强度加权、证据纯净）
- M2 卡点粒度：solution 分块 + "卡在第 N 步"自然语言标记
- M3 诊断模式：explain 带 user_answer，先定位后提示（治"抄答案"）
- M4 双向回忆卡片：词汇卡 + 方向（英↔中）独立调度 + contrasts/variant_of 邻接提示
- M5 真题衔接：真题覆盖门禁 + 池缺口报告
- M6 图补全：题目也可嵌图（problems.figure_paths）

**用户确认**：
> "好。就按你说的来。顺带一提，我认为这个系统真的要支持那种图床，就是得专门声明一些地方来专门存放图片。"

---

## 专题 13 · 图床与运行时目录原则

**用户决策（✅ A 修正）**：
> "A，但是我想说的一点是，这个程序所有的运行时文件都在这个隐藏文件夹里，就像其他配置文件夹一样，都是以英文句号开头，后面跟一个名字的文件夹。所以这里并不是一个Intermediate，下面再是Course文件夹。"

**确认（✅）**：图床 = `.lessonkit/figures/{course}/{chapter}/`（隐藏点目录原则；非 intermediate 树）；命名 `{owner_id}-fig-{NNN}.png`；池存逻辑路径。

**用户决策（✅ A）**：桥产物归位——`.lessonkit/explain/{course}/{chapter}/{item_id}.md`（跟踪资产）+ `.lessonkit/jobs/<id>/`（gitignored 瞬态）。

---

## 专题 14 · 前端决策点

**用户决策（✅）**：
- 视觉基调（自定义回答）：
> "就按照你所说的极简工具风来，你可以参考一下这个DeepSeek Harness它的前端的这个网页设计，反正就是特别简单就行。还有gpt-taste与UI Pro Max你也可以参考。"
- 首轮范围（自定义回答）：
> "当前好像有一个练习页和知识点展示页，这两个页都可以做出来。我的想法是：左边是选择栏目，中间是具体的页面，最右边是和AI Agent的对话页。"
- UI 语言：**中文界面**（✅）

**确认（✅）**：极简工具风；参考 DSH 前端（后升级为"照搬"，见专题 17）；首轮 = 练习页 + 知识点展示页；**三栏布局：左选择栏 / 中页面 / 右 AI 对话栏**。

---

## 专题 15 · 批量提问的七项回答

**用户决策（✅ 全部）**：
1. AI 上下文绑定：**只跟随不确认**
2. 揭晓节奏（自定义回答）：
> "我觉得可以统一作答完所有题再一次性显示，或者做完一题显示一题答案。而且我觉得，虽然说内容比较多，但界面上还是尽量简洁。如果是反馈，就只简单列一个弹窗，题目下面放一个反馈框就可以了，而且伴随答案出现，这个反馈框才出现。"
3. Agent 深度参与（自定义回答）：
> "然后我认为 Agent 还是要深度参与的，因为那样才方便整理。如果 Agent 暂时断联，那就只记录，不会有任何其他操作。这时候 Agent 可以看到这些做题记录的上下文，知道哪些题做了，哪些题还没有反馈、还没有被细化、还没有根据反馈进行题目入库出库的调整。然后 Agent 就可以根据反馈给这道题目贴标签、归类、放置之类的。"
4. 自评时机：**会话末统一自评**
5. 再练同类（自定义回答）：
> "我觉得不应该跟当前做题流程强绑定，因为它本质上是由当前的薄弱项延展出来的，属于生成题目或练习近似题的步骤，所以可以在会话末了。另外，学习记录也不应该强行绑定某个会话，其实我认为，理想的会话状态就是一个巨无霸的长对话，因为这样子就有那种AI教师一气呵成、从零开始陪伴的感觉。只是说我这样不太现实，但是我也没有更好的方案。。"
6. 服务生命周期：**固定端口 + 前台**
7. 工作区切换：**左栏下拉 + 列表**

**确认（✅）**：反馈框随答案出现（简单弹窗/题下反馈框）；Agent 深度参与（整理/贴标签/归类/放置，断联只记录）；记录不绑会话（池子为真源）；"巨无霸长对话"理想（现实妥协：池子+痕迹实现连续性）。

---

## 专题 16 · AI 全知优先制与极简主义

**用户原话（完整引用）**：
> "你可能还没有太理解，就是里面接入的AI Agent是全知全能的，它上下文其实可以看到所有东西，只是它当前用户看到的题目会被优先列出来，或者说用户最近看到的一些会被优先列出来。
> 这个'优先'你可以自己决定，比如说最近几道题目会作为重点来直接提供给 Agent，不代表它看不到其他已经看过的东西，Agent 都是知道的，只是不会作为明显上下文来直接绑定，Agent 依然可以自如地进行搜索来获知用户都干了什么。
> 还有，目前应该做得简陋一点，不需要弄很多东西。比如说既然有会话末统一自评，那摘要里就没有必要再拼一次。你要知道用户总是希望最简单的，他最讨厌的就是新增很多功能！！！！！！！！"

**确认（✅）**：AI Agent **全知**（可见一切，经 CLI 可搜索所有记录）；"跟随"只是**显示优先级**（当前题/最近题作为明显上下文），不是访问限制；**极简主义**（简陋一点、少功能；摘要不重复拼装；用户最讨厌新增功能）。

---

## 专题 17 · 前端批评与修正（两次）

**第一次批评（用户原话）**：
> "我觉得你连前端都需要展现什么，你自己心里都没数啊。就现在这样的前端，你觉得它有章法吗？你觉得它功能上面的逻辑说得通吗？而且我强调说过，你就仿照DeepSeek Harness的前端UI就可以了，你照搬你不会吗？"

**确认（✅）**：**照搬 DSH 前端 UI**（非参考）；前端要有章法、逻辑说得通。

**第二次批评（用户原话，完整引用）**：
> "不对，AI对话居然是右侧边栏，也就是右边那一栏。但是除此之外的确还有很多设计上的逻辑是说不通的，就比如说不是说左边侧边栏需要展示那个知识图谱嘛，还有那个练习，还有知识点那几个页嘛，这些我们前面讨论的难道你没有记住吗？？？？就是前面的我们讨论，你都需要记住！！！"

**确认（✅）**：**AI 对话 = 右侧边栏**（三栏布局中的右栏）；**左栏侧边栏需要展示：知识图谱、练习、知识点**等页面的导航入口；**前面讨论过的一切都必须记住**。

---

## 专题 18 · 记录要求（本文件缘起）

**用户原话（完整引用）**：
> "我觉得当务之急是，你只把前面我们讨论过的所有成果一字不落地记录下来，而不是凭感觉说我们可能讨论了些什么、重点在哪，然后只实现那些所谓的重点。"

**执行（✅）**：本文件。

---

## 附 · 已落地的产物索引

| 产物 | 内容 |
|---|---|
| `docs/adr/0009–0021` | 分层架构 / 挠痛处调度 / 弹性反馈 / AI 转接层 / 高信噪比输入 / 数据优先视图 / 级联信号 / 诊断模式 / 运行时资产布局 / 真题覆盖门禁 / 学习动作扩展 / Agent-Native 教学 / Scoropic（后置） |
| `openspec/specs/` | review-workbench（14 需求）/ ai-teacher-bridge（10）/ knowledge-figures（5）——已归档 |
| `openspec/changes/workbench-ui/` | 前端变更（进行中，待按本记录修正） |
| `docs/REQUIREMENTS.md` | 需求 + 边界 + 验收标准 |
| `docs/ARCHITECTURE.md` | 分层模块契约 |
| `AGENTS.md` | 兼容边界 + 开发纪律 + 验证命令 |
| `changelog/` | 2026-08-16 迁移与恢复 / 后端实现记录 |
| 后端实现 | `workbench/`（registry/data/domain/bridge/cli/server），148 测试全绿 |
